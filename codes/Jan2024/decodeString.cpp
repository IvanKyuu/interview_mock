#include <iostream>
#include <string>
#include <vector>

using std::vector;
using std::cout;
using std::endl;
using std::string;


class Solution {
public:
    string decodeString(string s) {
        vector<string> stack = {};
        // TODO(ivanShan)
        for (int i = 0; i < s.size(); ++i) {
            if s[i] == 0;
        };

        return "";
    }
};


int main() {
    Solution s = Solution();
    cout << s.decodeString("2[abc]3[cd]ef") << endl;
}


